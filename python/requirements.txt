# DICOM2NII Python 專案依賴套件
# 核心依賴

# DICOM 處理
pydicom>=2.3.0

# NIfTI 處理  
nibabel>=4.0.0

# DICOM 到 NIfTI 轉換
dcm2niix>=1.0.0

# 數值計算
numpy>=1.21.0

# 資料處理和分析
pandas>=1.5.0

# 並行處理 (Python 標準庫，但列出以說明版本需求)
# concurrent.futures - Python 3.7+

# HTTP 請求
requests>=2.28.0

# 雲端儲存 (AWS S3 相容)
boto3>=1.26.0
botocore>=1.29.0

# JSON 處理 (高效能)
orjson>=3.8.0

# 進度條
tqdm>=4.64.0

# 開發和測試依賴
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# 型別檢查
types-requests>=2.28.0

# 文件生成
sphinx>=5.0.0
sphinx-rtd-theme>=1.2.0

# 可選依賴 (用於特定功能)
# FastAPI 相關 (如果需要 Web API)
fastapi>=0.95.0
uvicorn>=0.20.0

# 資料庫 (如果需要)
asyncpg>=0.28.0
aiomysql>=0.2.0
sqlalchemy>=2.0.0

# Redis 快取 (如果需要)
redis>=4.5.0

# 監控和日誌
structlog>=22.0.0
